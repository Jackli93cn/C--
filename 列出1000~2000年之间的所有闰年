#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main22()
{
	int y = 0;
	for (y = 1000; y <= 2000; y++)
	{
		if (y % 4 == 0)
		{
			if (y % 100 != 0)
			{
				printf("%d ", y);
			}
		}
		if (y % 400 == 0)
		{
			printf("%d ", y);
		}
	}
	return 0;
}
